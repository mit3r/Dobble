#include "./index.hpp"

void GamePage::start(sf::RenderWindow& window) {
  (void)window;
}

void GamePage::end(sf::RenderWindow& window) {
  (void)window;
}

void GamePage::handleEvent(sf::RenderWindow& window, const sf::Event& event) {
  (void)window;
  (void)event;
}

void GamePage::render(sf::RenderWindow& window) {
  (void)window;
}
