#include "./index.hpp"

void EndPage::start(sf::RenderWindow& window) {
  (void)window;
}

void EndPage::end(sf::RenderWindow& window) {
  (void)window;
}

void EndPage::handleEvent(sf::RenderWindow& window, const sf::Event& event) {
  (void)window;
  (void)event;
}

void EndPage::render(sf::RenderWindow& window) {
  (void)window;
}
